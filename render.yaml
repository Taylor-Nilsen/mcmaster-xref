services:
  - type: web
    name: mcmaster-xref
    env: node
    plan: free
    rootDir: app
    buildCommand: npm install && npx playwright install --with-deps chromium
    startCommand: node server.js
