services:
  - type: web
    name: mcmaster-xref-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
